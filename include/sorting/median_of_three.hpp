#pragma once
#include "operation.hpp"

int median_of_three(const int* arr, size_t ax, size_t bx, size_t  cx, const operation& oper);

size_t median_of_three_index(const int* arr, size_t ax, size_t bx, size_t cx, const operation& oper);
